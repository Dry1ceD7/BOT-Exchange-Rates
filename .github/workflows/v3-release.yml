# ---------------------------------------------------------------------------
# BOT Exchange Rate Processor — V3.0.4 Cross-Platform Release Pipeline
# ---------------------------------------------------------------------------
# Triggers on version tags (v*). Builds standalone executables for
# Windows (.exe), macOS (.app), and Linux using PyInstaller --onedir.
# Uses uv for dependency management per working agreement mandate.
# ---------------------------------------------------------------------------

name: V3 Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # ── STAGE 1: Lint + Test ────────────────────────────────────────────────
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python 3.12
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --dev

      - name: Lint with ruff
        run: uv run ruff check .

      - name: Run tests
        run: uv run pytest tests/ -v

  # ── STAGE 2: Build Matrix ──────────────────────────────────────────────
  build:
    needs: quality-gate
    strategy:
      matrix:
        include:
          - os: windows-latest
            artifact: BOT-ExRate-Windows
          - os: macos-latest
            artifact: BOT-ExRate-macOS
          - os: ubuntu-latest
            artifact: BOT-ExRate-Linux
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Set up Python 3.12
        run: uv python install 3.12

      - name: Install dependencies
        run: |
          uv sync
          uv pip install pyinstaller

      - name: Build with PyInstaller
        shell: bash
        run: |
          uv run pyinstaller \
            --name "BOT-ExRate" \
            --onedir \
            --windowed \
            --hidden-import customtkinter \
            --hidden-import tkinterdnd2 \
            --hidden-import openpyxl \
            --hidden-import xlrd \
            --hidden-import httpx \
            --hidden-import pydantic \
            main.py

      - name: Package distribution
        shell: bash
        run: |
          cd dist
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            7z a -tzip BOT-ExRate-Windows.zip BOT-ExRate/
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            zip -r BOT-ExRate-macOS.zip BOT-ExRate/
          else
            tar -czf BOT-ExRate-Linux.tar.gz BOT-ExRate/
          fi

      - name: Upload Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            dist/BOT-ExRate-Windows.zip
            dist/BOT-ExRate-macOS.zip
            dist/BOT-ExRate-Linux.tar.gz
          retention-days: 30

  # ── STAGE 3: GitHub Release ────────────────────────────────────────────
  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: release-assets/**/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
