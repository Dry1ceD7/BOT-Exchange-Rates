---
description: Run the test suite for BOT_Exrate
---
# Run Tests

// turbo-all

1. Activate the virtual environment and run pytest:
```bash
cd /Users/d7y1ce/AAE/Projects/BOT_Exrate && source venv/bin/activate && python -m pytest tests/test_logic.py -v
```
